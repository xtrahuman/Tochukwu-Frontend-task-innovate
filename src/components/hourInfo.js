import React, { useEffect } from 'react';
import ErrorInfo from './errorInfo';
import Interactions from './interaction';


const HourInfo = () => {
//  const dispatch = useDispatch()

        // useEffect(() => dispatch(handleGet()));
    return (
  <div>
      <p>test</p>
    {/* <ErrorInfo />
    <Interactions/> */}
  </div>
)
};

export default HourInfo;