import React from 'react';
import ErrorInfo from './errorInfo';
import Interactions from './interaction';


const TodayInfo = ({dataDate, getInfo}) => {
    return (
  <div>
      <p>test</p>
    {/* <ErrorInfo />
    <Interactions/> */}
  </div>
)
};

export default TodayInfo;